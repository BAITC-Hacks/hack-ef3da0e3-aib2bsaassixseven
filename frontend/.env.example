# Browser-visible URL of the self-hosted FastAPI service.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Public product settings. Do not place secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_APP_NAME=Meeting Protocol
